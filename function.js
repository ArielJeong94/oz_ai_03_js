// 함수(Function)
// 코드를 재사용하기 위해 만들어놓은 코드 블록

// 함수 정의 -> "~ 기능을 하는 코드 덩어리"를 'X라는 이름'으로 부르겠다
function add(n1, n2) {
    // 함수 동작 구현부
    result = n1 + n2;
    return result;
};

// 함수 호출(call) -> 함수를 사용한다
add(1, 2);
console.log(result);